// import * as actionTypes from './constants'

const defaultStates = {
  recommendList: []
}

export default (state = defaultStates, action) => {
  switch(action.type) {
    default:
      return state
  }
}