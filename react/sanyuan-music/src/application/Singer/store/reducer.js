const defaultState = {
  category: "",
  alpha: "",
  singerList: [],
  enterLoading: true,
  listOffest: 0
}

export default (state = defaultState, action) => {
  switch(action.type) {
    default: 
      return state
  }
}